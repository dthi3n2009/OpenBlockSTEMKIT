# Ba thư mục dưới đây là repo git RIÊNG (fork của ThingEdu), không gộp vào đây.
# Người tiếp quản clone riêng từng cái — xem HANDOFF.md §4.
/gui/
/desktop/
/external-resources/

# Bộ công cụ tải về, không cần đưa lên git (xem cách cài lại trong docs/build-setup.md)
/tools/

# File cài đặt gốc của nhà sản xuất, rất nặng
/OpenBlock-Desktop_v1.0.2_win_x64/
/OpenBlockExternalResources.exe/

# Log build, sinh ra lúc chạy
/docs/compile-log.txt
/docs/dev-log.txt

# Bản sao main.js gốc của app (9 MB, chỉ để đối chiếu khi cần)
/app-mods/main.js.orig

node_modules/
*.log

# Bí mật và cấu hình chỉ dùng trên máy phát triển / máy chủ
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
/secrets/
/secret/
/config/api_keys.json
/config/local.json
/config/production.json
/server/.env*
/server/config/local.*
/server/config/production.*

# Dữ liệu cục bộ của học sinh, bản sao lưu và dữ liệu kiểm thử
*.dehoc
/userdata/
/userData/
/student-data/
/test-data/
/backups/
/uploads/
*.sqlite
*.sqlite3
*.db

# Kết quả build và cache có thể tạo lại
coverage/
.nyc_output/
dist/
out/
release/
build/
electron-dist/
*.asar
*.asar.unpacked
.npm/
.yarn/
.pnpm-store/
*.tsbuildinfo

# Flutter (chỉ dùng nếu sau này có bản Flutter)
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub/

# File hệ thống Windows và IDE
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
*.lnk
*.dmp
*.stackdump
.vscode/
.idea/
*.iml
*.swp
*.swo

# Công cụ kiểm thử nháp, không phải mã kiểm thử chính thức
/scratchpad/
/test-results/
/playwright-report/
/app-mods/test-*.js
*.tmp
*.orig
